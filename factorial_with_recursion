#include<stdio.h>
int fact(int);
int  main()
{
int num;
printf("enter a number");
scanf("%d",&num);
int res=fact(num);
printf("Factorial is %d",res);
return 0;
}
int fact(int num)
{
if(num==0||num==1)
return 1;
else
 return num*fact(num-1);
}

output:
enter a number 6
6
Factorial is 720
